# macOS
.DS_Store
.DS_Store?
._*

# Logs
*.log
logs/

# Environment files
.env
.env.*
!.env.example

# Plugin distribution source (not installed — active skills are in .claude/skills/)
/skills/

# Worktrees
.worktrees/

# run-tasks-p runtime state (subprocess logs + status files)
.claude/rtp/

# key for Claude
.key

# Workflow-test sandbox (created by tmp/erp-test/) — keep source, ignore deps + DB
tmp/**/node_modules/
tmp/**/*.db
tmp/**/*.db-journal
tmp/**/*.db-wal
tmp/**/*.db-shm